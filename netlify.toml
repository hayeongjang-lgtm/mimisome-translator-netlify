[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# 프론트엔드가 /api/translate 호출 → /.netlify/functions/translate 로 자동 라우팅
[[redirects]]
  from = "/api/translate"
  to = "/.netlify/functions/translate"
  status = 200
